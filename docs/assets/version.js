/* version.js — single source of truth for the displayed project version.
   Consumed by deck-chrome.js (deck menu) and index.html (landing title).
   Bump the version here only. Load this before any script that reads it. */
window.DLFC_VERSION = 'v0.5.2';
